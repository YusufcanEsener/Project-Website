<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.1.3/socket.io.js" integrity="sha512-PU5S6BA03fRv1Q5fpwXjg5nlRrgdoguZ74urFInkbABMCENyx5oP3hrDzYMMPh3qdLdknIvrGj3yqZ4JuU7Nag==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Canlı Sohbet Sayfası - Ateş Astan</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/profile.css">
    <link rel="stylesheet" href="/css/mesaj.css">
    <script src="/public/js/mesaj.js" defer></script>
    <style>
        .profile-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: rgba(0, 0, 0, 0.7);
            border-radius: 10px;
        }
        .profile-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .profile-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        .profile-box {
            background: #2b3245;
            padding: 20px;
            border-radius: 10px;
        }
        .logout-btn {
            background: #ff6f61;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <header class="header" id="baslangic">
        <a href="/">
            <img src="images/logo.png" alt="Logo" class="logo" >
        </a>
            <nav class="navbar">
                <a href="http://localhost:3000/#paketler" class="button">Uzaktan Eğitim Paketleri</a>
                <a href="http://localhost:3000/programlar" class="button">Programlar</a>
                <a href="http://localhost:3000/merak-edilenler" class="button">Merak Edilenler</a>
                <a href="http://localhost:3000/iletisim" class="button">İletişim</a>
            </nav>
            <% if (locals.user) { %>
                <% if (user.admin) { %>
                    <a href="/adminprofil"><i class="bi bi-person" style="position:relative;left: 100px;font-size: 20px;"> Admin Profil</i></a>
                <% } else { %>
                    <a href="/profile"><i class="bi bi-person" style="position:relative;left: 100px;font-size: 20px;"> Profilim</i></a>
                <% } %>
                <form action="/auth/logout" method="POST" style="display: inline;">
                    <button type="submit" class="logout-button">
                        <i class="bi bi-box-arrow-right" style="position:relative;left: 5px;font-size: 20px;color: white;"> Çıkış</i>
                    </button>
                </form>
            <% } else { %>
                <a href="http://localhost:3000/auth/login"><i class="bi bi-box-arrow-in-right" style="position:relative;left: 100px;font-size: 20px;"> Giriş Yap</i></a>
                <a href="http://localhost:3000/auth/register"><i class="bi bi-person" style="position:relative;left: 5px;font-size: 20px;"> Kayıt Ol</i></a>
            <% } %>
    </header>
        <div id="chat-wrap">
            <h2>Canlı Sohbet Kanalı</h2>
            <div id="chat-window">
                <div id="output"></div>
                <div id="feedback"></div>
            </div>
            <input type="text" id="sender" hidden>
            <input type="text" id="message" placeholder="Mesajınız">
            <button id="submitBtn">Gönder</button>
        </div>
        <script src="js/chat.js" charset="UTF-8"></script>
        <script>
            const urlParams = new URLSearchParams(window.location.search);
            const room = urlParams.get('room') || 'default-room';

            const socket = io('http://localhost:3001', {
                query: {
                    room: room
                }
            });

            var input = document.getElementById("message");
            input.addEventListener("keyup", function(event) {
            if (event.keyCode === 13) {
            event.preventDefault();
            document.getElementById("submitBtn").click();
            }
            });
        </script>
    </body>
</html>





